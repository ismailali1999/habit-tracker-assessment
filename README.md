# Habit Tracker

A single-page habit tracker built with React and Vite. Users can add daily habits, check them off on a weekly grid, navigate between weeks, and track current streaks.

## How to run locally

Install dependencies:

```bash
npm install